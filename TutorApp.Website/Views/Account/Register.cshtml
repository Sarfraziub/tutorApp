@model TutorApp.Website.ViewModels.RegisterViewModel
@{
    // Do not use any layout page
    Layout = null;
}
@* <div class="container d-flex justify-content-center align-items-center vh-100" style="background-color: #356374; width: 100%">
    <div class="text-center">
        <form asp-action="Register" method="post" class="w-100" style="max-width: 500px;">
            <div class="form-group mb-3">
                <input asp-for="Name" class="form-control" placeholder="Enter your full name"
                       style="padding: 10px; border-radius: 5px; border: 1px solid #ffffff;" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <input asp-for="Email" class="form-control" placeholder="Enter your email address"
                       style="padding: 10px; border-radius: 5px; border: 1px solid #ffffff;" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <input asp-for="Password" type="password" class="form-control" placeholder="Enter a strong password"
                       style="padding: 10px; border-radius: 5px; border: 1px solid #ffffff;" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <select asp-for="ClassId" class="form-select" style="padding: 10px; border-radius: 5px; border: 1px solid #ffffff;">
                    <option value="">-- Select your Class --</option>
                    @for (int i = 1; i <= 10; i++)
                    {
                        <option value="@i">Class @i</option>
                    }
                </select>
                <span asp-validation-for="ClassId" class="text-danger"></span>
            </div>

            <div class="d-flex justify-content-center gap-2 mt-3">
                <button type="submit" class="btn btn-primary" style="width: 120px;">Register</button>
            </div>
        </form>
        <div class="text-center mt-3">
            <small>Already have an account? <a asp-controller="Account" asp-action="Login">Login here</a></small>
        </div>
    </div>
</div> *@



<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #316b73;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .register-container {
            width: 350px;
            background-color: #f0f0f0;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .register-form {
            display: flex;
            flex-direction: column;
        }

        .register-form input,
        .register-form select {
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
        }

        .button-group {
            display: flex;
            justify-content: center;
        }

        .button-group button {
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
            background-color: #2e8b99;
            color: #fff;
        }

        .button-group button:hover {
            opacity: 0.9;
        }

        .text-danger {
            color: red;
            font-size: 12px;
        }

        .text-center {
            text-align: center;
        }

        .mt-3 {
            margin-top: 1rem;
        }

        .small-text {
            font-size: 0.9em;
        }
    </style>
</head>
<body>
    <div class="register-container">
        <form asp-action="Register" method="post" class="register-form">
            <input asp-for="Name" placeholder="Enter your full name" />
            <span asp-validation-for="Name" class="text-danger"></span>

            <input asp-for="Email" placeholder="Enter your email address" />
            <span asp-validation-for="Email" class="text-danger"></span>

            <input asp-for="Password" type="password" placeholder="Enter a strong password" />
            <span asp-validation-for="Password" class="text-danger"></span>

            <select asp-for="ClassId">
                <option value="">-- Select your Class --</option>
                @for (int i = 1; i <= 10; i++)
                {
                    <option value="@i">Class @i</option>
                }
            </select>
            <span asp-validation-for="ClassId" class="text-danger"></span>

            <div class="button-group mt-3">
                <button type="submit">Register</button>
            </div>
        </form>
        <div class="text-center mt-3">
            <small class="small-text">Already have an account? <a asp-controller="Account" asp-action="Login">Login here</a></small>
        </div>
    </div>
</body>
</html>

